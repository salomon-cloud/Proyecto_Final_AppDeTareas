import 'package:flutter/material.dart';

/// Colores utilizados en la aplicación
class AppColors {
  // Colores primarios
  static const primaryColor = Color(0xFF6366F1); // Indigo
  static const primaryLight = Color(0xFFE0E7FF);
  static const primaryDark = Color(0xFF4F46E5);

  // Colores de estado
  static const successColor = Color(0xFF10B981); // Verde
  static const errorColor = Color(0xFFEF4444); // Rojo
  static const warningColor = Color(0xFFF59E0B); // Naranja
  static const infoColor = Color(0xFF3B82F6); // Azul

  // Colores neutros
  static const backgroundColor = Color(0xFFFAFAFA);
  static const surfaceColor = Colors.white;
  static const borderColor = Color(0xFFE5E7EB);

  // Colores de texto
  static const textPrimary = Color(0xFF1F2937);
  static const textSecondary = Color(0xFF6B7280);
  static const textDisabled = Color(0xFFD1D5DB);
}
